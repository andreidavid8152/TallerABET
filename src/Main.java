import javax.swing.*;

public class Main {
    public static void main(String[] args) {
        //INTEGRANTES: ANDREI FLORES, JAIR RUEDA, LEONARDO TAMAYO

        //variable que almacenara a la App (GUI)
        App app = new App();
        app.setTitle("Rol de pagos");
        app.setSize(500, 500);
        app.setVisible(true);
        app.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}